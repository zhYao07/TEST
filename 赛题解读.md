# 浅层超稠油注采井连通性预测赛题解读

## 1. 赛题一句话概括

本赛题要求利用井组静态信息、注汽井日生产数据和采出井日生产数据，预测指定“井组—日期—注入指标—采出指标”样本的最佳响应滞后天数，输出范围为 0～45 天。

这不是一个普通的单目标回归问题。评分先把真实值和预测值划分为短滞后段与长滞后段，再分别评价两段内的回归误差。因此，更准确的任务定义是：

> 一个带有强分段评分、时间依赖、多井聚合和多指标耦合特征的时序预测问题。

本文只解读赛题，不包含模型训练、测试集预测或提交结果。

---

## 2. 业务问题与预测目标

### 2.1 业务机理

浅层超稠油黏度高、自然流动能力弱。注汽井向地层注入高温高压蒸汽后，蒸汽通过加热、降黏和补充地层能量等方式影响采出井。若注汽变化能较快反映到生产动态中，通常说明注采响应较快；若响应需要较长时间，则表示传递路径或响应过程较慢。

赛题使用“最佳滞后天数”量化这种动态响应：

- 滞后天数短：注入变化较快传递到采出端。
- 滞后天数长：采出端对注入变化的响应较慢。
- 允许取值：0～45 天。

需要注意，滞后天数主要反映“响应速度”，不能单独等同于连通强度。两个井组可能具有相同滞后，但响应幅度、稳定性和相关程度完全不同。因此，业务上解释结果时，最好把滞后与相关强度、响应增量和稳定性结合使用。

### 2.2 官方任务与业务愿景的边界

业务背景提到量化单口注汽井对采出井的贡献度，但官方监督标签的主键是：

`WELL_GROUP_NAME + PROD_DATE + INJ_INDICATOR + PROD_INDICATOR`

标签中没有注汽井 ID、采出井 ID，也没有单井贡献度。因此，本赛题直接考查的是井组层面的指标响应滞后，不是注汽井—采出井井对层面的贡献度预测。

可以利用井级数据构造井组特征，也可以在后续解释中估计单井影响，但仅凭官方标签无法直接监督学习每口注汽井的真实贡献率。竞赛结论不宜越界表述为已经解决了单井贡献度标定问题。

---

## 3. 样本到底是什么

### 3.1 一条标签记录的含义

一条样本表示：在某个井组、某个生产日期下，注入指标 `INJ_VOL_DAILY` 与某一个采出指标之间的最佳响应滞后天数。

当前数据中：

- 注入指标实际只有 1 个：`INJ_VOL_DAILY`。
- 采出指标有 5 个：
  - `LIQ_PROD_DAILY`
  - `OIL_PROD_DAILY`
  - `WATER_CUT`
  - `WH_TEMP`
  - `MAX_TUBING_PRES`

每个进入标签表的“井组—日期”固定对应 5 条记录，即分别预测注汽量相对 5 个采出指标的滞后。

因此，模型最终不是给每个井组输出一个滞后值，而是给每个井组日期输出 5 个可能不同的滞后值。

### 3.2 标签表的完整性

实际核对结果如下：

| 数据集 | 标签行数 | 井组—日期数 | 每个井组日期的标签数 | 井组数 | 标签日期范围 |
| --- | ---: | ---: | ---: | ---: | --- |
| 训练集 | 37,625 | 7,525 | 固定 5 条 | 108 | 2025-03-20～2026-02-28 |
| 测试集 | 11,020 | 2,204 | 固定 5 条 | 33 | 2026-03-01～2026-05-31 |

训练标签没有缺失，也没有发现标签主键重复。测试集 11,020 行的 `OPTIMAL_LAG_DAYS` 全部为空，正是需要填写的预测位置。

虽然训练原始动态数据从 2025-03-01 开始，但训练标签从 2025-03-20 才开始。测试原始动态数据覆盖 92 个自然日，标签只涉及其中 77 个日期，并非每个井组每天都有待预测记录。

---

## 4. 数据文件与字段解读

### 4.1 `well_group_info.csv`：井组与井的静态关系

| 字段 | 含义 | 使用注意事项 |
| --- | --- | --- |
| `WELL_GROUP_ID` | 井组 ID | 与井组名称一一对应，可作为类别或映射键 |
| `WELL_GROUP_NAME` | 井组名称 | 四张表的核心关联字段之一 |
| `WELL_ID` | 井 ID | 与井名对应，适合保留为标识字段 |
| `WELL_NAME` | 井名称 | 连接井级动态数据的重要字段 |
| `WELL_PURPOSE` | 井用途 | 包含注汽井、生产井 |
| `CANTON` | 区域 | 当前数据中为常量，直接建模价值很低 |
| `ORG_NAME` | 组织名称 | 当前只有 2 类 |
| `BLOCK` | 区块代码 | 当前只有 2 类，且与组织字段完全对应，二者信息高度重复 |
| `CONVERT_DATE` | 转驱日期 | 可计算相对样本日期的井龄或转驱时长 |
| `PROD_DATE` | 投产日期 | 当前文件中全部为同一天，不能与标签中的样本日期混为一谈 |

实际数据概况：

- 540 条井组—井关系。
- 112 个井组。
- 384 口唯一井。
- 425 条注汽井关系、115 条生产井关系。
- 平均每个井组约 3.80 口注汽井、1.03 口生产井。
- 153 口井同时属于多个井组，单井最多关联 3 个井组。
- 训练集与测试集的该文件完全相同，是一张全量静态主表。

同一物理井出现在多个井组通常代表共享影响关系。构造井组特征时不能在全局按 `WELL_NAME` 去重，否则会破坏井组拓扑。

### 4.2 `well_inj_data.csv`：注汽侧动态数据

| 字段 | 含义 | 单位/备注 |
| --- | --- | --- |
| `WELL_NAME` | 井名称 | 井级记录 |
| `WELL_GROUP_NAME` | 井组名称 | 同一井可能映射到多个井组 |
| `PROD_DATE` | 生产日期 | 日粒度 |
| `INJ_VOL_DAILY` | 日注汽量 | 立方米 |
| `INJ_LIQ_DAILY` | 日注液量 | 立方米 |
| `WH_TEMP` | 井口温度 | 摄氏度 |
| `STEAM_INJ_PRES` | 注汽压力 | 兆帕 |

数据核对结果：

| 指标 | 训练集缺失率 | 测试集缺失率 | 解读 |
| --- | ---: | ---: | --- |
| `INJ_VOL_DAILY` | 57.82% | 55.33% | 缺失较多，不能简单等同于 0 |
| `INJ_LIQ_DAILY` | 100% | 100% | 当前数据完全不可用，应剔除 |
| 注汽侧 `WH_TEMP` | 24.91% | 54.99% | 测试期缺失显著增加，存在分布漂移 |
| `STEAM_INJ_PRES` | 1.92% | 0.02% | 完整度较高 |

注汽数据训练集有 56,280 行、108 个井组；测试集有 4,408 行、42 个井组。原始动态表的井组范围大于或不同于标签表，不能直接把动态表中的全部井组都当作提交对象。

### 4.3 `well_prod_data.csv`：采出侧动态数据

| 字段 | 含义 | 单位/备注 |
| --- | --- | --- |
| `WELL_NAME` | 井名称 | 井级记录 |
| `WELL_GROUP_NAME` | 井组名称 | 核心关联字段 |
| `PROD_DATE` | 生产日期 | 日粒度 |
| `LIQ_PROD_DAILY` | 日产液量 | 立方米 |
| `OIL_PROD_DAILY` | 日产油量 | 立方米 |
| `WATER_CUT` | 含水率 | 百分比 |
| `WH_TEMP` | 井口温度 | 摄氏度 |
| `MAX_TUBING_PRES` | 最大油管压力 | 兆帕 |

数据核对结果：

- 训练集 80,443 行，测试集 14,996 行。
- 训练期日产液、日产油、含水率和采出侧井口温度的行级缺失率约为 41.5%。
- 测试期上述四项缺失率约为 31.8%。
- 最大油管压力缺失较少：训练集 3.49%，测试集 2.69%。
- 采出数据在训练和测试动态表中都覆盖 112 个井组，但测试标签只要求 33 个井组。

注汽表和采出表都含有 `WH_TEMP`。合并前必须重命名，例如 `INJ_WH_TEMP` 与 `PROD_WH_TEMP`，避免字段覆盖或错误使用。

### 4.4 `optimal_lag_days.csv` 与 `test_optimal_lag_days.csv`

| 字段 | 含义 |
| --- | --- |
| `WELL_GROUP_NAME` | 井组名称 |
| `PROD_DATE` | 样本日期 |
| `INJ_INDICATOR` | 注汽指标名称 |
| `PROD_INDICATOR` | 采出指标名称 |
| `OPTIMAL_LAG_DAYS` | 最佳滞后天数；训练集为标签，测试集待填写 |

这张表同时承担两个作用：

1. 定义训练监督样本。
2. 定义测试集必须输出的行、顺序和粒度。

提交时应以测试标签模板为基表，只填写最后一列。不要根据动态表自行生成提交行，也不要改变原行顺序。

---

## 5. 训练集与测试集的关系

### 5.1 不是完全陌生井组预测

训练标签包含 108 个井组，测试标签包含 33 个井组，其中 32 个井组在训练标签中已经出现，只有 `G112` 是测试标签中的新井组。

这意味着测试任务以同井组跨时间外推为主，而不是以全新井组泛化为主。井组历史、近期状态和井组类别特征很重要。不过，新井组仍需准备回退策略。

`G112` 只对应 2 个日期、10 条测试记录，占测试集约 0.09%，对总榜分影响很小。不能为了少量冷启动样本牺牲大量已见井组的时序预测效果。

### 5.2 各井组权重不相等

评分以样本行为单位，不按井组取平均。测试集中：

- 24 个井组各有 77 个日期，即各有 385 条记录。
- `G112` 只有 10 条记录。
- `G023`、`G024` 各有 25 条记录。
- 其他部分井组有 90～380 条记录。

覆盖日期多的井组对总分影响更大。离线评估应同时查看全局分数和分井组表现，但最终优化仍要尊重官方的逐行加权方式。

### 5.3 必须跨训练/测试边界拼接原始时间轴

测试期从 2026-03-01 开始，最大滞后为 45 天。若只读取测试动态数据，3 月初样本无法获得 2 月及更早的注采历史。

正确理解是：

- 训练、测试标签严格分开。
- 原始动态数据可按时间拼接，用训练期末的数据为测试期初构造历史窗口。
- 所有用于日期 `t` 的因果特征，只能使用在方案定义中允许的时间范围，避免无意引入未来信息。

### 5.4 数据分布存在变化

原始数值的粗略统计显示：

- 日注汽量非空记录均值由训练期约 53.85 上升到测试期约 75.76。
- 注汽压力均值由约 5.44 上升到约 6.34。
- 采出侧井口温度均值由约 90.91 上升到约 104.39。
- 注汽侧温度缺失率显著提高。

这些统计会受到测试井组构成变化影响，不能直接解释为全油田状态变化，但足以提示存在协变量漂移。模型不能只依赖训练期的绝对水平，变化率、相对量、井组内标准化和缺失模式也很重要。

---

## 6. 标签分布及其含义

训练标签范围完整覆盖 0～45 天，但分布高度不均匀：

| 统计量 | 数值 |
| --- | ---: |
| 样本数 | 37,625 |
| 均值 | 15.36 天 |
| 中位数 | 6 天 |
| 标准差 | 16.08 天 |
| 短滞后样本（0～3） | 17,820，47.36% |
| 长滞后样本（4～45） | 19,805，52.64% |

关键点位分布：

- 0 天：8,662 条，占 23.02%。
- 1 天：2,269 条，占 6.03%。
- 2 天：2,026 条，占 5.38%。
- 3 天：4,863 条，占 12.92%。
- 4 天：仅 29 条。
- 5 天：131 条。
- 6 天：1,315 条。
- 7 天：仅 1 条。
- 44 天：1,597 条。
- 45 天：673 条。

该分布不是平滑的普通回归分布，而是具有明显的分段和边界聚集。0、3、44、45 天出现大量样本，4、5、7、8 天非常稀疏。这可能来自真实生产状态的多模态特征，也可能与标签计算规则、搜索边界或筛选规则有关。

因此不宜默认采用均方误差把 0～45 当作均匀连续区间。均方误差会把少量大误差放大，并倾向输出两个高密度区域之间的中间值，而评分规则并不奖励这种折中。

### 6.1 不同采出指标的标签差异

| 采出指标 | 样本数 | 平均滞后 | 中位数 | 短滞后占比 |
| --- | ---: | ---: | ---: | ---: |
| `LIQ_PROD_DAILY` | 7,525 | 14.99 | 6 | 49.02% |
| `MAX_TUBING_PRES` | 7,525 | 13.92 | 3 | 51.67% |
| `OIL_PROD_DAILY` | 7,525 | 16.38 | 12 | 44.85% |
| `WATER_CUT` | 7,525 | 16.05 | 10 | 43.60% |
| `WH_TEMP` | 7,525 | 15.49 | 6 | 47.67% |

同一井组日期的 5 个标签并不相同：只有约 3.55% 的井组日期五个滞后值完全一致，只有约 22.52% 的井组日期五个指标全部落在同一短/长分段。因此不能给同一天的 5 个采出指标简单复制同一个预测值。

`PROD_INDICATOR` 必须作为核心条件变量。不同指标既可以分别建模，也可以共享主干后使用指标类别进行条件化。

---

## 7. 评分规则逐项解释

### 7.1 分段分类得分：满分 60 分

真实值和预测值都被划分为两个区间：

- 短滞后段：0～3 天。
- 长滞后段：4～45 天。

若真实值与预测值位于同一分段，则该样本分类正确：

$$
c_i=\mathbf{1}\left(G(y_i)=G(\hat{y}_i)\right)
$$

$$
\mathrm{Accuracy}_{seg}=\frac{1}{N}\sum_{i=1}^{N}c_i
$$

$$
S_{seg}=60\times\mathrm{Accuracy}_{seg}
$$

含义：分段是否正确占总分的 60%。分类准确率每提高 1 个百分点，综合得分提高 0.6 分。

### 7.2 短滞后段回归得分：满分 30 分

只在真实值属于 0～3 天的样本上计算平均绝对误差：

$$
\mathrm{MAE}_{short}=\frac{1}{N_{short}}\sum_{i\in I_{short}}|\hat{y}_i-y_i|
$$

$$
S_{short}=0.3\times\max\left(0,100-30\times\mathrm{MAE}_{short}\right)
$$

| 短段 MAE | 短段得分 |
| ---: | ---: |
| 0 | 30 |
| 0.5 | 25.5 |
| 1 | 21 |
| 2 | 12 |
| 3 | 3 |
| 3.33 及以上 | 约 0 |

短段误差代价非常高。在得分未触底时，短段 MAE 每降低 0.1 天，综合分约提高 0.9 分。

### 7.3 长滞后段回归得分：满分 10 分

只在真实值属于 4～45 天的样本上计算 MAE：

$$
\mathrm{MAE}_{long}=\frac{1}{N_{long}}\sum_{i\in I_{long}}|\hat{y}_i-y_i|
$$

$$
S_{long}=0.1\times\max\left(0,100-2\times\mathrm{MAE}_{long}\right)
$$

| 长段 MAE | 长段得分 |
| ---: | ---: |
| 0 | 10 |
| 5 | 9 |
| 10 | 8 |
| 20 | 6 |
| 30 | 4 |
| 40 | 2 |
| 45 | 1 |

长段回归误差的惩罚明显弱于短段。在有效区间内，长段 MAE 每降低 1 天，只增加 0.2 分。

评分说明正文中“长滞后样本数量为 `N_short`”应是文字笔误，紧随其后的公式分母使用的是 `N_long`，应按公式理解。

### 7.4 综合得分

$$
S_{final}=S_{seg}+S_{short}+S_{long}
$$

满分 100 分，其中：

- 短/长分段判断：60 分。
- 真实短滞后样本的精确预测：30 分。
- 真实长滞后样本的精确预测：10 分。

这套评分明确表达了三个优先级：

1. 首先把短、长滞后分对。
2. 其次把 0～3 天内部预测准。
3. 最后再改善 4～45 天内部的具体天数。

对于真实短滞后样本，若误预测为一个很大的长滞后值，会同时损失分段得分和短段 MAE 得分；对于真实长滞后样本，误预测到短段也会损失分段得分，但长段 MAE的附加惩罚较轻。因此分类阈值不一定以 0.5 为最优，必须在时间验证集上直接按照官方总分调节。

若短段得分尚未触底，综合指标可写成近似损失形式：

$$
100-S_{final}
=\frac{60}{N}\cdot\text{分段错误数}
+\frac{9}{N_{short}}\sum_{short}|\hat y-y|
+\frac{0.2}{N_{long}}\sum_{long}|\hat y-y|
$$

这个表达式直观说明：短段单样本的绝对误差权重远高于长段。

---

## 8. 榜单划分

- A 榜：使用 2026-03-01～2026-04-15 的预测结果，实时展示，用于调试。
- B 榜：使用 2026-03-01～2026-05-31 的全部预测结果，赛后统一公布，用于晋级和评奖。

实际测试模板中：

| 范围 | 行数 | 井组数 | 标签日期数 |
| --- | ---: | ---: | ---: |
| A 榜日期范围 | 6,715 | 31 | 46 |
| A 榜之后的日期 | 4,305 | 31 | 31 |
| B 榜全部范围 | 11,020 | 33 | 77 |

B 榜不是只计算 4 月 16 日之后的样本，而是计算完整测试期。不能把 A 榜最优参数直接视为 B 榜最优参数，也不应围绕公开榜反复过拟合。

赛题基线为 60 分。这里的“60 分”是晋级门槛或官方基线，不代表只要做好 60 分的分类项就会自然得到 60 分。用训练标签进行简单常数预测检查时，最好的固定整数预测为 1 天，训练综合分约 52.68，说明常数策略不足以越过门槛。

---

## 9. 赛题的核心难点

### 9.1 多层级数据无法直接一行合并

标签是井组—日期—指标粒度，动态数据是井—井组—日期粒度。一个井组日期可能有多口井记录。若直接按井组和日期连接注汽表、采出表与标签表，会形成多对多笛卡尔积，造成重复样本和数值放大。

合理的数据层级应为：

1. 保留井级动态记录。
2. 按业务含义构造井级时间特征。
3. 再汇总成井组日期特征，或用能够处理多实例输入的结构聚合。
4. 最后与标签主键一对一连接。

### 9.2 多井聚合口径不同

不同指标不能全部用同一种聚合函数：

- 日注汽量、日产液量、日产油量通常具有可加性，可优先考虑求和。
- 含水率是比例，不宜直接相加，可按产液量加权或由汇总液油量派生。
- 温度和压力可考虑均值、最大值、加权均值、离散度和井间极差。
- 还应保留有效井数、开井数、缺失井数、各井贡献占比等拓扑特征。

### 9.3 缺失与停井不能混为一谈

数据中既存在真实的 0，也存在空值。0 可能表示停注、停产或测量为零；空值可能表示未采集、指标不适用或该井当日无记录。统一用 0 填补会人为制造突变和虚假相关。

应至少保留：

- 原值及缺失标记。
- 距离上次有效观测的天数。
- 窗口内有效观测比例。
- 活跃井数与停井状态。

### 9.4 目标具有明显时间依赖

相邻有标签日期的上一条真实标签与当前标签落在同一短/长分段的比例约为 91.48%，完全相等的比例约为 70.56%。这说明标签在局部时间上较稳定。

但“使用上一条真实标签预测下一条”的训练内一步实验并不是可部署方案，因为测试期不会逐日公开真实标签。这个现象主要用于提醒：随机切分会把相邻、高度相似的样本放到训练和验证两侧，得到虚高分数。

### 9.5 原始互相关不等于最终答案

从业务上看，滞后与注采序列的互相关峰值天然相关。但实际数据包含趋势、停井、缺失、多井叠加、共同季节变化和非线性响应。仅取一条原始 Pearson 互相关曲线的最大值，容易把共同趋势或边界峰误判为真实传递滞后。

互相关更适合作为一组特征或候选滞后，而不是未经校准的唯一预测规则。需要同时考虑去趋势、差分、秩相关、峰值稳定性、有效样本数和监督模型修正。

### 9.6 冷启动与井组漂移并存

测试集几乎都是历史已见井组，但生产状态可能已经改变。过度依赖井组 ID 会记住旧状态，完全不用井组 ID 又会丢失地质和井网差异。更合理的思路是同时保留：

- 井组身份或历史先验。
- 近期动态和状态变化。
- 井组拓扑、区块、井数等可迁移特征。
- 新井组的全局或相似井组回退规则。

---

## 10. 适合本赛题的建模问题拆解

本节只解释合理的问题结构，不实施训练。

### 10.1 推荐的分层任务

可以将最终预测拆成三部分：

1. 二分类：判断真实滞后是否属于 0～3 天。
2. 短段预测：在 0、1、2、3 中预测具体天数。
3. 长段预测：在 4～45 中预测具体天数。

最终根据短段概率和经过验证集调优的阈值，选择短段或长段输出。分段后必须限制输出范围：短段结果限制在 0～3，长段结果限制在 4～45，避免四舍五入后跨段。

### 10.2 为什么短段更适合离散建模

短段只有 4 个整数值，而且分值对 MAE 极敏感。将其建模为四分类或有序分类，再根据概率选择条件中位数，通常比无约束连续回归更符合 MAE目标。

### 10.3 为什么长段适合稳健回归

长段范围宽、分布不均、边界点较多，但只有 10 分权重。长段模型应优先稳定，使用与 MAE一致的绝对误差或分位数目标，并限制输出在 4～45。无需为少量长段误差牺牲分段准确率和短段精度。

### 10.4 统一模型与分模型可以并存

可比较三种结构：

- 单一 46 分类模型。
- 二阶段“短/长分类 + 段内模型”。
- 统一回归、分段模型和信号算法的融合。

最终选择应由严格时间验证得分决定，而不是仅比较普通 RMSE 或总体 MAE。

---

## 11. 特征理解框架

### 11.1 当前状态特征

- 当日各项注采指标。
- 有效井数、开井数、停井数。
- 多井汇总值、均值、最大值、最小值、标准差和极差。
- 注汽量在各井之间的集中度。
- 缺失模式与距上次有效观测的时间。

### 11.2 时序变化特征

- 1、2、3、7、14、30、45 天滞后值。
- 一阶差分、百分比变化、变化方向。
- 3、7、14、30、45 天滚动均值、标准差、极值、斜率和变异系数。
- 连续停注、恢复注汽、突增、突降等事件特征。
- 采出端对应的增量、拐点和异常波动。

### 11.3 注采耦合特征

对于每个采出指标，计算注汽量与采出序列在 0～45 天候选滞后下的关系，例如：

- Pearson、Spearman 或稳健相关系数。
- 原序列、差分序列、去趋势序列的互相关。
- 最大相关对应的候选滞后。
- 最大相关强度。
- 第一峰与第二峰的差距。
- 峰值是否位于 0、3、44、45 等边界。
- 邻近窗口中候选滞后的稳定性。
- 每个候选滞后的有效配对样本数。

除目标采出指标外，其他四个采出指标也可作为上下文。例如温度先响应、产液后响应时，指标之间的先后关系可能帮助判断状态。

### 11.4 静态与拓扑特征

- 井组内注汽井数、生产井数。
- 井组内有动态数据的井数。
- 井龄、转驱时长及其统计量。
- 区块、组织等类别。
- 同一物理井关联井组数。
- 历史短滞后比例、历史条件中位数等井组先验。

历史标签统计只能用训练折之前的数据计算。若直接用全训练集统计再做时间验证，会把验证期标签泄漏进特征。

### 11.5 指标特定处理

- `LIQ_PROD_DAILY`、`OIL_PROD_DAILY`：优先保留总量及井间分布。
- `WATER_CUT`：考虑产液加权值及由液油量推导的含水指标，同时检查口径一致性。
- `WH_TEMP`：区分注汽侧与采出侧，可关注升温幅度与温度传播。
- `MAX_TUBING_PRES`：关注水平、波动、突变与压力响应。
- `INJ_LIQ_DAILY`：当前训练和测试均全空，不应进入模型。

---

## 12. 验证方案是本赛题的关键

### 12.1 不应采用普通随机行切分

普通随机切分存在三类风险：

1. 同一井组的相邻日期进入训练和验证，时间状态几乎相同。
2. 同一井组日期的 5 个指标被拆到两侧，共享相同原始窗口。
3. 由全量时间序列计算的滚动或相关特征可能使用验证日期之后的数据。

这会使本地得分显著高于真实跨期预测能力。

### 12.2 推荐的主验证方式

主验证应采用滚动时间验证，例如：

- 用更早月份训练、12 月验证。
- 扩大训练窗口、1 月验证。
- 再扩大训练窗口、2 月验证。

每折都要完整重算只依赖当时可用数据的特征。若使用 45 天滞后和较长滚动窗口，可设置相应的时间隔离区，防止高度重叠窗口带来的乐观偏差。

### 12.3 冷启动辅助验证

可以增加按井组留出的辅助验证，用于检查新井组回退能力。但由于正式测试只有 10 条 `G112` 冷启动记录，辅助验证不能取代时间验证，也不应获得与主任务相同的优化权重。

### 12.4 验证指标必须完全复刻官方公式

每折至少报告：

- `Accuracy_seg`
- `MAE_short`
- `MAE_long`
- `S_seg`
- `S_short`
- `S_long`
- `S_final`
- 分生产指标得分
- 分井组得分和样本量

模型选择、分类阈值、段内后处理和融合权重都应以 `S_final` 为主，而不是 RMSE、普通 MAE或分类 AUC。

---

## 13. 数据处理中的高风险点

### 13.1 日期格式不统一

不同文件中日期字符串存在补零和时间格式差异，例如 `2026/3/1 0:00` 与 `2026/04/01 00:00:00`。应统一解析为日期类型并归一到日粒度，不能用原始字符串连接。

### 13.2 多对多连接导致样本膨胀

必须在连接标签前检查中间表的唯一键。最终特征表在
`WELL_GROUP_NAME + PROD_DATE + INJ_INDICATOR + PROD_INDICATOR`
上应唯一，并与标签行数完全一致。

### 13.3 不能按井用途强行过滤动态表

实际核对发现，注汽动态表中也存在静态用途为生产井的记录，采出动态表中也存在静态用途为注汽井的记录。这可能与转驱或历史用途变化有关。

因此，动态文件本身的指标语义应作为主要依据，`WELL_PURPOSE` 更适合作为特征或核验字段，而不是无条件删除记录的硬规则。

### 13.4 标签井组与动态井组范围不一致

- 训练标签中的 `G049`、`G073` 在训练注汽动态表中没有同名井组记录。
- 训练和测试动态表都包含若干不在对应标签表中的井组。
- 测试采出动态表覆盖全部 112 个井组，但提交只需要 33 个井组。

应明确回退逻辑，并始终由标签模板控制训练或提交范围。缺失井组不能在内连接时被静默丢失。

### 13.5 行顺序不能假设已经排序

测试标签文件并未按所有指标字段的字典序严格排序。任何排序、透视或合并操作后，都应使用稳定的原始行号恢复模板顺序。

### 13.6 预测值范围与类型

提交值应按竞赛要求处理为 0～45 天。标签为整数，建议最终输出整数，并执行：

1. 短段预测裁剪到 0～3。
2. 长段预测裁剪到 4～45。
3. 四舍五入或按验证最优规则离散化。
4. 最终再次裁剪到 0～45。

---

## 14. 对基线与本地结果的正确理解

训练标签上的简单诊断表明：

- 最优固定整数预测约为 1 天，训练综合分约 52.68。
- 相邻标签的一步“上一真实值”预测可达到约 71.73，但它使用了测试时不可获得的前一条真实标签，只能证明局部稳定性，不能作为正式方案。
- 将很长一段未来时期都固定为训练期最后一个标签，得分会明显下降，说明局部持续性不能直接外推几个月。

由此可得到三点解读：

1. 只利用总体标签分布不足以超过 60 分。
2. 只依赖井组历史最后值也不稳健，状态切换是关键难点。
3. 随机切分或一步滚动验证容易给出虚高结果，必须用多步时间外推验证。

---

## 15. 建议的赛题分析路线

以下是适合正式做题阶段的顺序，本文件不执行这些步骤。

### 阶段一：数据与评分基线

- 固化官方评分函数。
- 完成主键、日期、缺失、重复和范围检查。
- 建立常数、全局中位数、分指标中位数等无信息基线。
- 建立严格时间验证框架。

### 阶段二：可解释信号基线

- 正确完成井组日聚合。
- 构造注采互相关候选滞后及峰值特征。
- 对原序列、差分序列和去趋势序列分别检验。
- 比较不同窗口长度和缺失处理口径。

### 阶段三：评分对齐的监督模型

- 训练短/长分类器。
- 训练 0～3 短段模型。
- 训练 4～45 长段模型。
- 在折外预测上联合调分类阈值与输出离散化规则。

### 阶段四：稳健性与融合

- 检查不同月份、井组和采出指标的稳定性。
- 比较信号算法、树模型和序列模型的互补性。
- 只融合在多个时间折上稳定提升的方案。
- 为缺失注汽历史和新井组建立明确回退策略。

### 阶段五：提交核验

- 行数必须为 11,020。
- 五个字段名和模板一致。
- 前四列逐行与测试模板一致。
- `OPTIMAL_LAG_DAYS` 无缺失、为合法数值、范围在 0～45。
- 不增加索引列，不改变编码和行顺序。
- 同时保存 A 榜分段分析与完整 B 榜方案版本，避免只围绕公开榜调参。

---

## 16. 最终结论

本赛题最容易被误解为“对 0～45 天做普通回归”，但真正决定成绩的是以下五点：

1. **任务是井组—日期—采出指标级预测。** 每个井组日期有 5 个不同目标，不是每个井组一个值。
2. **评分以短/长分段为核心。** 60 分来自分段判断，短段精度再占 30 分，长段精度只占 10 分。
3. **短段错误代价极高。** 模型结构、阈值和后处理必须直接围绕官方总分设计。
4. **这是跨时间外推。** 32 个测试井组在训练中出现，但随机切分会严重高估能力；主验证必须按时间推进。
5. **数据工程与模型同等重要。** 多井聚合、日期拼接、缺失语义、井组范围、指标口径和提交顺序任一处理错误，都可能比换模型造成更大损失。

超过 60 分的关键不是堆叠最复杂的网络，而是先把样本粒度、时间边界、评分权重和数据关联做对，再用严格验证判断哪些动态特征真正能够识别短/长状态及其具体滞后。
