# 中石油比赛建模交接文档

> 可将本文直接交给下一个 Codex 对话。工作目录为 `F:\中石油比赛`，使用 conda 的 `NLP` 环境。

## 一、必须持续遵守的约束

1. 客观使用赛题数据和 EDA 图表，但不要把 `EDA/08_EDA结论与建模约束.md` 中的建模约束当成必须遵守的方案。
2. 特征按小步实验增加，每次只引入一组容易解释的变化，并与当前最好版本比较。不要一次堆很多特征。
3. 验证集固定为 2025-12-01 至 2026-02-28，代码中使用左闭右开的区间 `[2025-12-01, 2026-03-01)`。
4. 所有代码在 conda 的 `NLP` 环境运行。
5. **每次修改建模代码、特征代码、预测规则、参数或依赖时，必须同步修改 `submission_code/` 中对应代码和配置，并重新生成 `submission_code.zip`。不能只修改 `modeling/`。**
6. 同步关系至少包括：
   - `modeling/get_dataset.py` 的特征逻辑 → `submission_code/src/feature.py` 和必要的 `submission_code/config.yaml`。
   - `modeling/model.py` 的推理、阈值和解码规则 → `submission_code/src/model.py` 和 `submission_code/config.yaml`。
   - 重训后的最终模型 → `submission_code/models/*.cbm`。
   - 环境或运行方法变化 → `submission_code/requirements.txt` 和 `submission_code/README.md`。
   - 同步后重新打包并检查压缩包能从原始 `data/` 独立生成结果。
7. 模型训练脚本只保存最终提交 CSV、最终模型权重和一份 JSON 汇总，不要恢复验证预测、特征重要性、分月明细、分指标明细和追加日志等大量 CSV。
8. 最终结果文件统一命名为 `test_optimal_lag_days.csv`。

## 二、数据与 EDA 状态

- 原始数据位于 `data/train/` 和 `data/test/`。
- 赛题及字段说明见 `赛题解读.md`。
- 完整 EDA 位于 `EDA/`，总报告是 `EDA/EDA总报告.md`，图表位于 `EDA/figures/`，统计表位于 `EDA/tables/`。
- 井组结构以多注汽井、单生产井为主，注汽侧最多可有 8 口井；同一物理井可能属于多个井组。因此底表按井组聚合，并保留多种聚合视角。
- 标签范围为 0～45。0～3 与 4～45 之间存在明显分段特征，所以尝试过分层模型。

## 三、当前数据底表

数据构造脚本：`modeling/get_dataset.py`。

底表粒度是：

```text
WELL_GROUP_NAME × PROD_DATE
```

注汽侧和生产侧的原始日记录先映射到井组，再按井组和日期聚合。每个指标保留：

```text
sum, mean, max, min, std, valid_well_count
```

另外保留注汽侧和生产侧的 `recorded_well_count`。底表补齐井组自然日，历史窗口按自然日计算。缺失值不做前向填充；全缺失的 sum 保持 NaN，计数为 0。

当前生成文件位于 `modeling/dataset/`：

- `group_day.csv.gz`
- `train.csv`
- `validate.csv`
- `test.csv`
- `test_template.csv`
- `dataset_info.json`

当前数据形状：

| 数据 | 形状 |
|---|---:|
| group-day | 51,184 × 58 |
| train | 28,575 × 230 |
| validate | 9,050 × 230 |
| test | 11,020 × 229 |

动态历史特征在完整时间线上连续计算。另有 9 个 label prior 特征：Train 严格使用样本日前标签；Validate 冻结在 2025-12-01 之前；Test 冻结在 2026-03-01 之前。

## 四、当前历史特征版本

当前版本名：

```text
same_day_aggregates_hist7_14_30_60_90_selected8_label_prior_v1
```

只对以下 8 个核心序列计算历史统计：

1. `inj__INJ_VOL_DAILY__sum`
2. `inj__WH_TEMP__mean`
3. `inj__STEAM_INJ_PRES__mean`
4. `prod__LIQ_PROD_DAILY__sum`
5. `prod__OIL_PROD_DAILY__sum`
6. `prod__WATER_CUT__mean`
7. `prod__WH_TEMP__mean`
8. `prod__MAX_TUBING_PRES__max`

窗口为：

```text
7, 14, 30, 60, 90 天
```

每个窗口计算：

```text
mean, std, min, max
```

所有窗口先 `shift(1)`，即目标日 `t` 的特征最多使用到 `t-1`，不包含当天。8 个序列 × 5 个窗口 × 4 个统计量，共增加 160 个历史特征；底表原有的当日多聚合特征仍保留。

曾加入以下变化特征：

```text
diff_1, diff_3, diff_7
slope_7, slope_14, slope_30
```

这组特征后来被移除，已恢复到当前最好版本。下个对话不要误把它们加回去，除非作为新的独立对照实验。

## 五、做过的实验与结果

### 1. 仅 group-day 多聚合底表的三阶段基线

当时数据形状为：

```text
train / validate / test = (28575, 61) / (9050, 61) / (11020, 60)
```

终端记录：

```text
segment 实际树数: 1
short 实际树数: 116
long 实际树数: 1
```

验证结果：

```text
n                 = 9050
segment_accuracy  = 0.4879558011
MAE_short         = 13.0734375
MAE_long          = 20.1186180
S_seg             = 29.2773481
S_short           = 0
S_long            = 5.9762764
score             = 35.2536245
```

这个结果表明仅使用当天聚合值时，分段模型几乎没有学到稳定的时间关系，segment 和 long 都在第 1 棵树停止。

### 2. 调整短段路由阈值

把路由阈值改为 `0.4` 后，用户口头记录验证分数提升到约 `49`。当前目录没有保留该次完整指标或训练汇总，因此它只能作为实验线索，不能当作精确可复现结果。

### 3. 逐步加入历史窗口

最初只考虑 7 天的 mean、std、min、max。因为若对全部底表列展开会产生大量特征，后来明确收缩为上述 8 个核心序列。

之后将窗口扩展为 7、14、30、60、90 天，并尝试 diff/slope。diff/slope 版本没有保留，当前回到不含 diff/slope 的多窗口版本。

无 label prior 的多窗口安全 Long 基线已经重跑：固定 0.5 阈值时得分约 53.54；在同一当前数据上重现并扫描阈值后，最好为 53.74635，阈值仍为 0.5。

### 4. 冻结历史 label prior v1

按 `WELL_GROUP_NAME × PROD_INDICATOR` 增加 9 个历史标签画像：count、last lag、last short、历史均值/中位数/short 比例、最近 5 条中位数/short 比例、距离最后标签天数。

- Train：每行严格使用 `date < t` 的标签。
- Validate：全部冻结在 2025-12-01 之前。
- Test：全部冻结在 2026-03-01 之前。
- 阈值扫描范围 0.20～0.80，步长 0.01；该版本最优阈值为 0.56。

验证结果：

```text
segment_accuracy = 0.6177901
MAE_short        = 1.9015625
MAE_long         = 24.3291747
S_seg            = 37.0674033
S_short          = 12.8859375
S_long           = 5.1341651
score            = 55.0875059
树数             = segment 20 / short 51 / long 30
```

分月总分为：2025-12 约 76.30、2026-01 约 51.43、2026-02 约 51.71。Train 的 label prior 年龄中位数为 1 天，而 Validate 为 61 天，说明冻结先验老化仍是下一步要解决的问题。

### 5. Train 三个月 block-frozen prior 实验

已将 Train 划分为三个冻结块：2025-03-20～05-31、2025-06-01～08-31、2025-09-01～11-30，各块只使用块起点前的标签画像。块内 8 项画像恒定，只有 `label_days_since_last` 变化；防泄露和历史计数校验全部通过。Train prior 年龄中位数从 1 天提高到 54 天，接近 Validate 的 61 天。

但该方案没有胜出：

```text
固定阈值 0.56：46.3328241
该版本最佳阈值 0.50：52.1831035
树数：segment 3 / short 76 / long 10
```

纯 block-frozen 抹掉了训练期有用的局部标签持续性，Segment 再次只保留 3 棵树。实验产物保存在 `modeling/outputs/catboost_dec_feb_safe_long4_label_prior_block3m_v1/`，但当前代码、数据集和提交包已恢复到胜出的 past-only v1。

### 6. 三阶段模型中的 Long 输出策略

原始结构为：

```text
X → Short/Long → Short 四分类或 Long 回归
```

- 第一阶段：CatBoostClassifier，预测 `y <= 3` 或 `y >= 4`。
- Short：CatBoostClassifier 四分类，类别为 0、1、2、3；根据条件分布中位数输出，以最小化期望绝对误差。
- Long：CatBoostRegressor，损失为 MAE。

实验发现 Long 回归常输出约 30 天，一旦真实短段样本被错误路由到 Long，会造成非常大的短段 MAE。当前策略保留 Long 模型权重，但进入 Long 路由后固定输出安全值 `4`。

### 7. 单模型方案

已新建 `modeling/model2.py`，使用一个 `CatBoostRegressor(loss_function='MAE')` 直接预测全部 0～45，不做三阶段路由。预测裁剪到 `[0, 45]` 后用 `np.rint` 转为整数。

本对话没有留下 `model2.py` 的明确验证分数，因此不能宣称它优于当前三阶段方案。

## 六、当前代码状态

### `modeling/model.py`

当前关键配置以代码为准：

```text
SHORT_THRESHOLD       = 0.56
LONG_SAFE_VALUE       = 4
EARLY_STOPPING_ROUNDS = 150
iterations            = 1000
depth                 = 7
learning_rate         = 0.02
l2_leaf_reg           = 5
random_seed           = 42
```

训练流程：

1. train 拟合，十二月、一月、二月验证并早停。
2. 记录各阶段选定树数和使用的特征列。
3. 合并 train 与 validate，按选定树数全量重训。
4. 预测 test。

当前只保存：

- `test_optimal_lag_days.csv`
- `final_models/segment.cbm`
- `final_models/short.cbm`
- `final_models/long.cbm`
- `stage_diagnostics.json`
- `training_summary.json`

### `modeling/model2.py`

当前关键配置：

```text
EARLY_STOPPING_ROUNDS = 80
iterations            = 1000
depth                 = 7
learning_rate         = 0.01
l2_leaf_reg           = 5
loss_function         = MAE
random_seed           = 42
```

当前只保存：

- `test_optimal_lag_days.csv`
- `final_model.cbm`
- `training_summary.json`

两个脚本已经移除验证预测、验证模型、分月/分指标 CSV、特征重要性 CSV、测试预测明细和追加训练日志。验证成绩仍打印到终端并写入 `training_summary.json`。

## 七、submission_code 状态

提交代码包位于：

```text
submission_code/
├── README.md
├── predict.py
├── requirements.txt
├── config.yaml
├── src/
│   ├── __init__.py
│   ├── feature.py
│   ├── model.py
│   └── utils.py
├── models/
│   ├── segment.cbm
│   ├── short.cbm
│   └── long.cbm
└── examples/
    └── result_example.json
```

压缩包为 `submission_code.zip`。当前压缩包已检查，共 12 个必要文件。

当前提交包配置：

```yaml
short_threshold: 0.56
long_safe_value: 4
label_prior_recent_observations: 5
history_windows: [7, 14, 30, 60, 90]
```

`submission_code/predict.py` 从赛事原始 `data/` 读取数据，一次性完成预处理、特征构造、模型加载、推理和结果生成。它只写入 `--output` 指定的最终 CSV，不保存中间文件。

运行方式：

```powershell
conda run -n NLP python submission_code/predict.py --data_dir ./data --output ./test_optimal_lag_days.csv
```

重新打包：

```powershell
Compress-Archive -Path 'submission_code\*' -DestinationPath 'submission_code.zip' -Force
```

## 八、当前产物提醒

- 当前工作区存在 `submission_code.zip` 和 `submission_code/models/*.cbm`。
- 当前项目根目录没有 `test_optimal_lag_days.csv` 和 `result.zip`。
- 当前胜出产物位于 `modeling/outputs/catboost_dec_feb_safe_long4_label_prior_v1/`。
- 提交模型、0.56 阈值、冻结 prior 推理代码和 README 已同步；`submission_code.zip` 已重建并解压独立运行，结果与建模输出逐行完全一致。
- 当前测试预测分布为：0 天 255、1 天 197、2 天 283、3 天 280、4 天 10005。

## 九、建议下一个对话先做的事

1. 不要重复纯三个月 block-frozen Train 实验；它的最优成绩只有 52.18310。
2. 若继续改造 label prior，应尝试保留 past-only 信号同时模拟老化的混合训练方式，而不是整块完全冻结。
3. 另一个独立方向是加入严格因果的注汽—目标采出指标互相关特征，不要与 label-prior 混合实验同时加入。
4. 对比 `model2.py` 的统一回归验证成绩，确定三阶段安全解码是否确实更好。
5. 后续每次只增加一类特征，并记录：特征版本、验证总分、分月总分、S_seg、S_short、S_long、MAE_short、MAE_long、树数和运行参数。
6. 任何胜出版本都要立刻同步到 `submission_code/`，复制对应最终模型权重，独立运行预测，校验五列字段、行数、顺序、整数范围 0～45，然后重新生成 `submission_code.zip`。

## 十、提交格式

最终 CSV 必须包含且仅包含以下五列，字段名称和顺序与测试模板完全一致：

```text
WELL_GROUP_NAME
PROD_DATE
INJ_INDICATOR
PROD_INDICATOR
OPTIMAL_LAG_DAYS
```

`OPTIMAL_LAG_DAYS` 必须为 0～45 的整数，不得缺失。平台要求结果文件命名为 `test_optimal_lag_days.csv`，通常打包为 `result.zip` 上传；代码包为 `submission_code.zip`。
